name: draft a release on github (testrepo)

on:
  workflow_dispatch: {}

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ls -alh > /dev/stderr
      - uses: https://git.k-fortytwo.de/actions/gh-release@e5f02f6e7629078bac913cff901bbb9515961091
        with:
          repo: christofsteel/testrepo
          tag: gh-testrelease
          auth-token: ${{ secrets.GH_TESTREPO_CONTENTS_WRITE }}
          commitish: main
          files: pyproject.toml
